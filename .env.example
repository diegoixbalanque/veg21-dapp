# ============================================
# VEG21 dApp Environment Variables
# ============================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in the required values below
# 3. NEVER commit your .env file to Git (it's in .gitignore)
#
# For local development, you can leave most values empty
# and the app will run in demo mode (no blockchain interactions)
#
# ============================================

# ===== DATABASE CONFIGURATION =====

# PostgreSQL Database URL (Neon/Replit database)
# Format: postgresql://user:password@host:port/database
# Leave empty for demo mode (uses localStorage)
DATABASE_URL=

# ===== SERVER CONFIGURATION =====

# Port for the Express server (default: 5000)
PORT=5000

# Node environment (automatically set by package.json scripts)
# NODE_ENV=development  (for npm run dev)
# NODE_ENV=production   (for npm start)

# ===== APPLICATION MODE =====

# VEG21_MODE controls whether the app uses mock data or real blockchain
# Options:
#   - demo: Mock mode with localStorage simulation (DEFAULT - safest for development)
#   - celo-alfajores: Celo Alfajores testnet (requires wallet + testnet CELO)
#   - celo-mainnet: Celo mainnet (REAL MONEY - requires wallet + real CELO)
#   - local: Local Hardhat network (requires running hardhat node)
#
# ⚠️ IMPORTANT: Set to 'demo' for local development to prevent accidental transactions!
VITE_VEG21_MODE=demo

# ===== BLOCKCHAIN DEPLOYMENT CONFIGURATION =====
# (Only needed when deploying contracts or running in real mode)

# Private Key for Contract Deployment
# ⚠️ CRITICAL SECURITY WARNINGS:
#   - Use a DEDICATED deployment wallet (never your personal wallet!)
#   - NEVER commit .env file with real private keys
#   - Store securely in password manager
#   - For testnet, use a wallet with only test CELO
#
# Format: 64 hexadecimal characters WITHOUT '0x' prefix
# Example: abcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890
# Leave EMPTY for demo mode (no deployment)
PRIVATE_KEY=

# Celo Alfajores Testnet RPC URL
# Default uses public Forno endpoint (no API key needed)
# For better performance, consider using a dedicated RPC provider (Alchemy, Infura, etc.)
CELO_ALFAJORES_RPC_URL=https://alfajores-forno.celo-testnet.org

# Celo Mainnet RPC URL
# Default uses public Forno endpoint
# ⚠️ Use dedicated RPC for production to avoid rate limits!
CELO_MAINNET_RPC_URL=https://forno.celo.org

# CeloScan API Key (required for automatic contract verification)
# Get FREE API key at: https://celoscan.io/myapikey
# Not needed for demo mode, but required for contract deployment
CELOSCAN_API_KEY=

# ===== DEPLOYED CONTRACT ADDRESSES =====
# (Populated automatically after deployment or set manually)

# Celo Alfajores Testnet Contract Addresses
# Leave empty until you deploy to Alfajores testnet
# Format: 42-character hex string starting with 0x
VITE_CELO_ALFAJORES_TOKEN_ADDRESS=
VITE_CELO_ALFAJORES_STAKING_ADDRESS=
VITE_CELO_ALFAJORES_DONATIONS_ADDRESS=
VITE_CELO_ALFAJORES_REWARDS_ADDRESS=

# Celo Mainnet Contract Addresses
# Leave empty until mainnet deployment
# ⚠️ Double-check these addresses before mainnet launch!
VITE_CELO_MAINNET_TOKEN_ADDRESS=
VITE_CELO_MAINNET_STAKING_ADDRESS=
VITE_CELO_MAINNET_DONATIONS_ADDRESS=
VITE_CELO_MAINNET_REWARDS_ADDRESS=

# ===== OPTIONAL: CUSTOM RPC ENDPOINTS =====
# (Override default RPCs for better performance)

# Alchemy, Infura, or other RPC provider URLs
# Format: https://provider.com/v2/YOUR_API_KEY
# VITE_RPC_URL=

# ===== QUICK START GUIDE =====
#
# 🚀 LOCAL DEVELOPMENT (Demo Mode):
#   1. Keep VITE_VEG21_MODE=demo
#   2. Run: npm run dev
#   3. Open: http://localhost:5000
#   4. All data stored in browser localStorage (no blockchain)
#
# 🧪 TESTNET DEPLOYMENT (Alfajores):
#   1. Get testnet CELO: https://faucet.celo.org
#   2. Set PRIVATE_KEY (deployment wallet)
#   3. Set CELOSCAN_API_KEY
#   4. Run: npx hardhat run scripts/deploy.ts --network celo-alfajores --execute
#   5. Copy contract addresses to VITE_CELO_ALFAJORES_* variables
#   6. Set VITE_VEG21_MODE=celo-alfajores
#   7. Restart app: npm run dev
#
# 🚀 MAINNET DEPLOYMENT (Celo):
#   1. Complete ALL testnet testing first!
#   2. Review DEPLOYMENT_CHECKLIST.md and REVIEW_AND_VERIFY.md
#   3. Fund deployment wallet with 5-10 CELO
#   4. Run: npx hardhat run scripts/deploy.ts --network celo-mainnet --execute
#   5. Copy contract addresses to VITE_CELO_MAINNET_* variables
#   6. Set VITE_VEG21_MODE=celo-mainnet
#   7. Deploy to production!
#
# ===== SECURITY REMINDERS =====
#
#   ✅ DO:
#     - Use dedicated deployment wallet
#     - Store private keys in password manager
#     - Keep .env in .gitignore
#     - Use demo mode for local development
#     - Test thoroughly on Alfajores before mainnet
#
#   ❌ DON'T:
#     - Commit .env to Git
#     - Share private keys
#     - Use personal wallet for deployment
#     - Deploy to mainnet without thorough testing
#     - Reuse the same private key across testnets and mainnet
#
# ============================================
# Last Updated: October 2025
# VEG21 - Gamifying Vegan Lifestyle on Celo
# ============================================
